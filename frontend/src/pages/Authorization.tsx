import React from 'react';

const Authorization: React.FC = () => {
    return (
        <form>
            <input type='email' />
            <input type='password' />
        </form>
    );
};

export default Authorization;
